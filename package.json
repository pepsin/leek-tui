{
  "name": "leek-tui",
  "version": "1.0.0",
  "description": "A minimal terminal UI for tracking stocks and funds — TypeScript edition",
  "main": "dist/index.js",
  "bin": {
    "leek-tui": "dist/index.js"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["stock", "fund", "tui", "terminal", "finance"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
